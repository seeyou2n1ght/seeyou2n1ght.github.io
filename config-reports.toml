# 报告保存配置
# 默认报告保存路径：博客仓库的 posts 目录

[report]
  default_save_path = "/home/ilvzhengwei/.openclaw/workspace/blog-seeyou2n1ght/content/posts/"
  backup_path = "/home/ilvzhengwei/.openclaw/workspace/"
  
  # 报告类型配置
  [report.types]
    rpa_industry = "rpa-industry-report-{date}.md"
    openclaw_evolution = "openclaw-evolution-report-{date}.md"
    twitter_daily = "twitter_daily_report_{date}_{time}.md"
    pi_mono = "pi-mono-analysis-{date}.md"
    
  # 自动发布配置
  [report.publish]
    auto_commit = true
    auto_push = true
    commit_message = "Add report: {filename}"
    branch = "main"